spring:
  cloud:
    config:
      name: crawler-config
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: crawler-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6660/eureka/

